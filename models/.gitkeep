# This file ensures the directory structure is preserved in Git
# Large model files are excluded by .gitignore
# Users should download the segmentation model separately (see README.md) or contact 202120274@spu.ac.za if the uploaded file does not run properly.
